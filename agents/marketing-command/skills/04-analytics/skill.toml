[skill]
name = "Marketing Analytics"
version = "1.0"
description = "Track and report on marketing performance metrics"
triggers = ["analytics", "metrics", "performance", "ROI", "report"]
tools = ["http_request", "memory_save", "memory_search"]
